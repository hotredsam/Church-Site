---
interface Props {
  title: string;
  intro?: string;
}
const { title, intro } = Astro.props;
---
<section class="section">
  <div class="container">
    <p class="kicker">Kaleo Church</p>
    <h1 class="page-title">{title}</h1>
    {intro && <p class="lead">{intro}</p>}
  </div>
</section>
