{
  "name": "kaleo-church-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "sync:legacy": "node ./scripts/sync-legacy-pages.mjs",
    "sync:content-map": "node ./scripts/generate-content-map.mjs",
    "check:links": "node ./scripts/check-links.mjs"
  },
  "dependencies": {
    "astro": "^5.2.0"
  }
}
